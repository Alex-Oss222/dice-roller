# Reviewed preparation baseline

On 22 September 2026 the user authorized repository-wide workflow implementation and cleanup. This replaces the earlier prepared baseline on main at `c37fd39cec980440b8e83f3f4b5df3238cc1960a`. That commit preserves the prior source files and manifest.

Story1 has no initialized campaign or accepted turns to migrate. Its manifest is deliberately updated after validation to pin the new shared engine, workflow, context/rendering, conservative travel policy and common instructions. This is reviewed common maintenance, not a story turn or permission to bypass future baseline drift. No second story exists to migrate.

The distance catalog and original user character/style inputs remain byte-for-byte preserved. The starting character's current preparation and setup include separately documented player-authorized changes; the manifest's imported-sheet digest remains historical import provenance, not an edit lock or the current character state.

Obsolete blank-sheet audit and baseline snapshot files were removed during authorized cleanup. Their exact earlier contents are available at the prior Git commit above. The hashes in story.json identify the adopted baseline, while Git preserves the actual files.

After initialization, a future upgrade needs an explicit compatibility review for the existing event chain. Ordinary turns may not rewrite these pins.
